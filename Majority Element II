class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        count = defaultdict(int)
        res = []
        for n in nums:
            count[n] += 1
        
        for i,c in count.items():
            if c > len(nums)/3:
                res.append(i)
        return res
